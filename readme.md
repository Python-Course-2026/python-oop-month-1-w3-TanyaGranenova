## 🚀 Шпаргалка по ООП на Python (Дни 1–7)
## 📅 День 1: Магические методы (Dunder Methods)

##  Позволяют объектам взаимодействовать со встроенными функциями Python (print, len, +, sort).

| Метод      | За что отвечает                            | Пример вызова     |
| ---------- | ------------------------------------------ | ----------------- |
| `__init__` | Конструктор: создание атрибутов            | `obj = MyClass()` |
| `__str__`  | Строка для пользователя                    | `print(obj)`      |
| `__repr__` | Техническая строка (для отладки и списков) | `[obj1, obj2]`    |
| `__add__`  | Сложение: логика знака `+`                 | `obj1 + obj2`     |
| `__lt__`   | Сравнение: логика знака `<`                | `obj1 < obj2`     |


## 📅 День 2: Инкапсуляция и @property

Защита данных от некорректного изменения
(например, нельзя установить отрицательную цену).

Пример реализации:
```python
class Product:
    def __init__(self, price):
        self.__price = price  # Приватный атрибут

    @property
    def price(self):
        """Геттер: чтение данных"""
        return self.__price

    @price.setter
    def price(self, value):
        """Сеттер: проверка перед записью"""
        if value < 0:
            raise ValueError("Цена не может быть ниже нуля!")
        self.__price = value
```


## 📅 День 3–4: Списки объектов и аналитика

Работа с коллекциями (агрегация).
Когда один главный класс управляет списком других объектов.

## Пример реализации:
```python
class Warehouse:
    def __init__(self):
        self.items = []  # Список для хранения объектов

    def add_item(self, item):
        self.items.append(item)

    def get_total_value(self):
        # Суммируем атрибут 'price' у всех объектов в списке
        return sum(obj.price for obj in self.items)
```

## 📅 День 5: Наследование (Inheritance)

### Создание подклассов, которые расширяют или изменяют логику родителя.

### Ключевые инструменты:

```python 
super().__init__(...) 
```
— вызвать конструктор родителя

## Переопределение метода — написать метод с тем же именем, но новой логикой

Пример:
```python
class Employee:
    def __init__(self, name, salary):
        self.name = name
        self.salary = salary


class Developer(Employee):
    def __init__(self, name, salary, bonus):
        super().__init__(name, salary)  # Передаем данные родителю
        self.bonus = bonus

    def get_total_pay(self):
        return self.salary + self.bonus
```

## 📅 День 6–7: Взаимодействие систем

## Логика, где один объект влияет на состояние другого
(сделки, переводы).

## Пример логики перевода:

```python
def transfer(from_acc, to_acc, amount):
    # Проверяем возможность списания у первого объекта
    if from_acc.withdraw(amount):
        # Если успешно, пополняем второй объект
        to_acc.deposit(amount)
        return "Успех"
    return "Ошибка"```